# Infrastructure Directory

Reserved for infrastructure definitions (compose overrides, deployment manifests, service templates).

Current repository still uses root `Dockerfile` and `docker-compose.yml` for compatibility.
