# 🚀 Инструкция по загрузке PRIZMBET на Netlify

## 📋 Содержание
1. [Быстрый деплой (без GitHub)](#быстрый-деплой)
2. [Деплой с автообновлением (через GitHub)](#деплой-с-github)
3. [Проверка работы](#проверка)

---

## 🎯 Способ 1: Быстрый деплой (без GitHub)

**⚠️ Внимание:** При этом способе данные НЕ будут обновляться автоматически!

### Шаг 1: Подготовка проекта
```bash
cd "C:\Users\GravMix\Desktop\suite full stake\prizmbet-final"
```

Убедитесь, что в папке есть:
- ✅ `index.html`
- ✅ `matches.json`
- ✅ `netlify.toml`
- ✅ Папка `netlify/functions/`
- ✅ Все изображения (логотипы, QR-код)

### Шаг 2: Загрузка на Netlify

1. **Откройте Netlify Drop**
   - Перейдите: https://app.netlify.com/drop
   - Или зарегистрируйтесь/войдите на https://app.netlify.com

2. **Перетащите папку**
   - Просто перетащите папку `prizmbet-final` в окно браузера
   - Или используйте кнопку "Browse to upload"

3. **Дождитесь деплоя**
   - Netlify автоматически:
     - Загрузит файлы
     - Настроит serverless функции
     - Создаст уникальный URL

4. **Готово!**
   - Вы получите ссылку вида: `https://random-name-123.netlify.app`
   - Можно изменить на своё имя: Site settings → Change site name

### ⚠️ Ограничения этого способа:
- ❌ Данные НЕ обновляются автоматически
- ❌ Нужно вручную загружать новый `matches.json`
- ✅ Но serverless функция работает (можно обновлять вручную)

---

## 🔄 Способ 2: Деплой с автообновлением (через GitHub)

**✅ Рекомендуется:** Данные будут обновляться автоматически каждые 3 часа!

### Шаг 1: Создание GitHub репозитория

1. **Откройте GitHub**
   - Перейдите: https://github.com
   - Войдите в аккаунт (или создайте новый)

2. **Создайте новый репозиторий**
   - Нажмите "+" → "New repository"
   - Название: `prizmbet` (или любое другое)
   - Выберите: **Public** (для бесплатного GitHub Actions)
   - **НЕ** добавляйте README, .gitignore, license
   - Нажмите "Create repository"

### Шаг 2: Загрузка проекта в GitHub

Откройте PowerShell/Терминал:

```bash
cd "C:\Users\GravMix\Desktop\suite full stake\prizmbet-final"

# Проверьте, что у вас есть git
git --version

# Если нет - установите: https://git-scm.com/download/win
```

Выполните команды (замените YOUR_USERNAME на ваш GitHub username):

```bash
# Проверить текущее состояние
git status

# Добавить remote (замените YOUR_USERNAME!)
git remote add origin https://github.com/YOUR_USERNAME/prizmbet.git

# Проверить remote
git remote -v

# Отправить код
git push -u origin master
```

**Если просит авторизацию:**
- Логин: ваш GitHub username
- Пароль: используйте **Personal Access Token** (не обычный пароль!)
  - Создать токен: https://github.com/settings/tokens
  - Выберите: "Generate new token (classic)"
  - Permissions: `repo` (полный доступ к репозиториям)
  - Скопируйте токен и используйте вместо пароля

### Шаг 3: Подключение к Netlify

1. **Войдите в Netlify**
   - https://app.netlify.com
   - Можно войти через GitHub аккаунт

2. **Создайте новый сайт**
   - Нажмите "Add new site" → "Import an existing project"

3. **Подключите GitHub**
   - Выберите "GitHub"
   - Авторизуйте Netlify (если первый раз)
   - Выберите репозиторий `prizmbet`

4. **Настройки деплоя**
   ```
   Branch to deploy: master
   Build command: (оставьте пустым)
   Publish directory: .
   ```

5. **Environment Variables (не обязательно)**
   - Если хотите использовать свой Google Sheet:
   ```
   SHEET_ID = ваш_sheet_id
   SHEET_GID = 0
   ```

6. **Нажмите "Deploy site"**
   - Первый деплой займёт 1-2 минуты
   - Вы получите URL: `https://minortermite.github.io/betprizm`

### Шаг 4: Настройка автообновления

GitHub Actions уже настроен! Он автоматически:
- ✅ Запускается каждые 3 часа (00:00, 03:00, 06:00, 09:00, 12:00, 15:00, 18:00, 21:00 UTC)
- ✅ Скачивает данные из Google Sheets
- ✅ Обновляет `matches.json`
- ✅ Делает commit и push
- ✅ Netlify автоматически пересобирает сайт

**Проверить работу Actions:**
1. Перейдите в репозиторий на GitHub
2. Вкладка "Actions"
3. Увидите запуски каждые 3 часа

**Запустить вручную:**
1. Вкладка "Actions"
2. Выберите "Auto-update matches from Google Sheets"
3. Нажмите "Run workflow" → "Run workflow"

---

## 🎨 Шаг 3: Настройка домена (опционально)

### Изменить название сайта

1. Site settings → General → Site details
2. Change site name
3. Введите своё имя: `prizmbet` → `minortermite.github.io/betprizm`

### Подключить свой домен

1. **Купите домен** (например, на Namecheap, REG.RU)

2. **В Netlify:**
   - Site settings → Domain management
   - Add custom domain
   - Введите домен: `prizmbet.com`

3. **Настройте DNS:**
   - У регистратора домена добавьте записи:
   ```
   Type: A
   Name: @
   Value: 75.2.60.5
   
   Type: CNAME
   Name: www
   Value: minortermite.github.io/betprizm
   ```

4. **SSL сертификат:**
   - Netlify автоматически выдаст SSL (HTTPS)
   - Подождите 10-30 минут

---

## ✅ Проверка работы

### 1. Проверить основной сайт
- Откройте: `https://minortermite.github.io/betprizm`
- Должны загрузиться матчи
- Проверьте фильтры по спортам
- Проверьте поиск

### 2. Проверить Netlify Function
```bash
# Откройте в браузере:
https://minortermite.github.io/betprizm/.netlify/functions/update-matches
```

Должен вернуть JSON:
```json
{
  "last_update": "2026-02-16 12:00:00",
  "matches": [...]
}
```

### 3. Проверить GitHub Actions (если используете)
1. GitHub → репозиторий → Actions
2. Должны быть зелёные галочки
3. Можно запустить вручную: Run workflow

### 4. Проверить логи Netlify
1. Netlify → ваш сайт → Deploys
2. Кликните на последний deploy
3. Проверьте логи на ошибки

---

## 🔧 Устранение проблем

### Проблема: "Site not found"
- Подождите 2-3 минуты после деплоя
- Очистите кэш браузера (Ctrl+Shift+Delete)

### Проблема: Матчи не загружаются
1. Проверьте `matches.json` в репозитории
2. Проверьте console браузера (F12)
3. Убедитесь, что файл не пустой

### Проблема: GitHub Actions не работает
1. Проверьте Settings → Actions → General
2. Включите: "Allow all actions and reusable workflows"
3. Запустите вручную: Actions → Run workflow

### Проблема: Netlify Function возвращает 502
1. Проверьте логи: Netlify → Functions → Logs
2. Возможно проблема с Google Sheets:
   - Проверьте, что таблица открыта для просмотра
   - URL должен быть правильным

### Проблема: Данные не обновляются
1. GitHub Actions работает? (проверьте Actions)
2. `matches.json` обновляется? (проверьте коммиты)
3. Netlify rebuilds автоматически? (проверьте Deploys)

---

## 📊 Мониторинг

### GitHub Status Badge
Добавьте в README.md:
```markdown
![Auto Update](https://github.com/YOUR_USERNAME/prizmbet/workflows/Auto-update%20matches%20from%20Google%20Sheets/badge.svg)
```

### Netlify Status Badge
1. Netlify → Site settings → Status badges
2. Скопируйте markdown код
3. Добавьте в README.md

---

## 🎯 Итоговая структура

```
GitHub Repository (prizmbet)
    ↓
    ↓ (каждые 3 часа)
    ↓
GitHub Actions запускает update_matches.py
    ↓
    ↓ (скачивает CSV)
    ↓
Google Sheets → matches.json
    ↓
    ↓ (commit & push)
    ↓
GitHub уведомляет Netlify
    ↓
    ↓ (auto rebuild)
    ↓
Netlify пересобирает сайт
    ↓
    ↓
Пользователи видят новые данные
```

---

## 📱 Контакты и поддержка

- **Telegram**: https://t.me/+PMrQ9Nbzu08wYmI0
- **Кошелек PRIZM**: PRIZM-4N7T-L2A7-RQZA-5BETW

---

## 🎉 Готово!

После деплоя ваш сайт будет:
- ✅ Доступен 24/7
- ✅ Обновляться автоматически каждые 3 часа
- ✅ Иметь SSL сертификат (HTTPS)
- ✅ Работать быстро (Netlify CDN)
- ✅ Адаптирован под мобильные

**Ссылка на сайт:** `https://minortermite.github.io/betprizm`

Удачи! 💎🚀
